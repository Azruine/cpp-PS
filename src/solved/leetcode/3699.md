# 3699. [Number of ZigZag Arrays I](./3699.cpp)

마지막 숫자를 기준으로 해서 내려가게 끝나는 것과 올라가게 끝나는 것은 대칭이다.

dp 테이블은 다음과 같이 생각한다 - 첫 인덱스는 길이, 다음 인덱스는 종료한 숫자

$dp[i][x][up] = sum_{v=1}^{x-1} dp[i-1][v][down] = sum_{v=m-x+2}^{m} dp[i-1][v][up]$

이런 식으로 된다.

그래서 배열 두 개와 prefix sum 을 가지고 $O(NM)$으로 풀어낼 수 있다.
